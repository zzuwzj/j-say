# J-Say 环境变量配置

# 后端 API 地址（J-Say 前端同步语料时使用）
# 配置后，应用启动时会自动从服务端同步已发布的语料数据
# 未配置时，前端使用本地内置数据，不影响正常使用
VITE_API_BASE=http://localhost:3001

# 后端服务端口
PORT=3001

# 管理员密码
ADMIN_PASSWORD=admin123

# CORS 允许的来源
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# 导入条数上限
IMPORT_LIMIT=5000